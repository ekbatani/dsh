services:
  deepseek-harness:
    build: .
    restart: unless-stopped
    ports:
      - "${DSH_PORT:-3000}:3000"
    environment:
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
    volumes:
      - dsh-data:/home/node/.dsh
      - ./workspace:/workspace

volumes:
  dsh-data:
